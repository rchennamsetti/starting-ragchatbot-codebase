---
description: Run the project's test suite
---

Run the project's tests.

1. Inspect the repository configuration to identify the appropriate test command.
2. Run the complete test suite (for example, `pytest` for a Python project).
3. Report the command used and summarize the results, including any failures and relevant error messages.

Do not modify project files while running the tests.
